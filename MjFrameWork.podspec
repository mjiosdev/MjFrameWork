Pod::Spec.new do |s|
  s.name         = "MjFrameWork"
  s.version      = "0.0.1"
  s.summary      = "A short description of MjFrameWork."
s.description  = <<-DESC
A much much longer description of MyFramework.
DESC
  s.homepage     = "http://EXAMPLE/MyFramework"
  s.license      = ""
  s.author       = { "bruceMJ" => "mjdev@live.in" }
  s.source       = { :path => '.' }
  s.source_files  = "MjFrameWork/*.swift"

end
